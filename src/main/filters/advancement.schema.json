{
	"$schema": "${schema.schema.url}",
	"title": "Minecraft Advancement ${version.minecraft} Draft ${project.version}",
	"description": "Minecraft datapack Advancement object schema for version ${version.minecraft}",
	"$ref": "${schema.url}/main.schema.json#/definitions/advancement"
}
