{
	"$schema": "${schema.schema.url}",
	"title": "Minecraft Tag ${version.minecraft} Draft ${project.version}",
	"description": "Minecraft datapack Tag object schema for version ${version.minecraft}",
	"$ref": "${schema.url}/main.schema.json#/definitions/tag"
}
