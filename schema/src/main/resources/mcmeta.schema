{
	"$schema": "${url.schema.json}",
	"title": "Minecraft datapack Metadata ${version.minecraft} Draft ${project.version}",
	"description": "Minecraft datapack Metadata object schema for version ${version.minecraft}",
	"$ref": "${url.schema.minecraft}#/definitions/mcmeta"
}
